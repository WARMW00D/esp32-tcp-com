# ESP32-C3 Cisco Console Server

Удалённый COM-порт для консольного доступа к сетевому оборудованию (Cisco и совместимому) через Wi-Fi. ESP32-C3 + MAX3232 превращают локальный консольный порт устройства в TCP-сокет, доступный из сети — без физического подключения ноутбука к каждому свитчу/маршрутизатору.

> 🤖 Прошивка и веб-портал разработаны в паре с **Claude** (Anthropic) — от первой версии моста до финальной настройки безопасности, тёмной темы и WPA2-Enterprise.

## Возможности

- **TCP ⇄ UART мост** на порту `8888` (по умолчанию), с буферизованной пересылкой блоками и минимальным Telnet-согласованием (`IAC WILL ECHO`), чтобы клиент не дублировал набранные команды.
- **Одна активная сессия** — вторая попытка подключения получает `BUSY`, пока не закрыта первая (или её не «выбьют» из веб-портала).
- **Веб-портал** на порту `80` в тёмной теме, с переключателем языка **RU/EN**.
- **Настраиваемая скорость UART** (1200–115200 бод) — меняется на лету, без перезагрузки.
- **Резервная точка доступа**: если целевая сеть недоступна или не настроена, устройство поднимает собственную AP на `192.168.4.1`.
- **Настройка Wi-Fi через портал**: сканирование сетей, выбор SSID из списка или ручной ввод (для скрытых сетей).
- **WPA2-Enterprise (802.1X)** — подключение к офисным/корпоративным сетям через PEAP/MSCHAPv2 (Identity/Username/Password), без CA-сертификата.
- **Пароль на сессию** — опциональная защита TCP-порта паролем, который клиент вводит при подключении.
- **White List сетей** — ограничение подключения по IP/CIDR (например, `192.168.1.0/24`).
- **Static IP** — фиксированный адрес на целевой Wi-Fi сети (по желанию, DHCP по умолчанию).
- **MAC-адрес** устройства виден на портале — в режиме AP и в режиме STA (пригодится для MAC-фильтрации на роутере).
- **Автоматический ретрай радио** — если первая попытка подключения к Wi-Fi не удалась, прошивка сама выключает и включает радиомодуль перед повторной попыткой (борется с «зависшим» после софт-рестарта Wi-Fi).
- **Пониженная мощность передачи** (`WIFI_POWER_8_5dBm`) — снижает пиковый ток на TX-всплесках, что заметно повышает стабильность на платах со слабым питанием (актуально для дешёвых клонов ESP32-C3 SuperMini).
- **Опциональная блокировка по IP** после 3 неверных попыток пароля (компилируется флагом, см. ниже).
- Все настройки хранятся в **NVS** (энергонезависимая память ESP32) и переживают перезагрузку.

## Железо

| Компонент | Назначение |
|---|---|
| ESP32-C3 Super Mini | основная плата |
| MAX3232 (RS-232 ⇄ TTL) | согласование уровней с консольным портом Cisco |
| Кабель Cisco console (RJ45 ⇄ DB9) | подключение к устройству |

### Распиновка

| ESP32-C3 | MAX3232 |
|---|---|
| GPIO20 (RX1) | TXD |
| GPIO21 (TX1) | RXD |
| 3V3 / GND | питание платы MAX3232 |

⚠️ Дешёвые платы ESP32-C3 SuperMini нередко страдают от просадок питания в момент передачи по Wi-Fi (особенно в режиме точки доступа). Если видите нестабильную работу Wi-Fi или устройство иногда само перезагружается — добавьте конденсатор 470–1000 мкФ между 3V3 и GND как можно ближе к плате.

## Установка

1. Откройте `esp32-tcp-com.ino` в Arduino IDE (или PlatformIO) с установленным пакетом плат **ESP32 by Espressif** (используются `WiFi.h`, `WebServer.h`, `Preferences.h` — все входят в стандартный core, дополнительных библиотек не требуется).
2. Выберите плату **ESP32C3 Dev Module**.
3. При желании поменяйте заводские значения по умолчанию в начале файла:
   ```cpp
   const char* DEFAULT_SSID     = "your_ssid";
   const char* DEFAULT_PASSWORD = "your_password";
   const char* apSsid     = "ESP32-Console";
   const char* apPassword = "console1234";
   ```
   Это лишь стартовые значения — реальные рабочие настройки после первого сохранения через портал хранятся в NVS и переживают перепрошивку (если не стирать флеш полностью).
4. Залейте прошивку.

## Первый запуск

- Если целевая сеть недоступна или не настроена, устройство поднимет собственную сеть **`ESP32-Console`** (пароль `console1234`) на адресе **`192.168.4.1`**.
- Подключитесь к этой сети и откройте `http://192.168.4.1` в браузере.
- Перейдите в раздел **Wi-Fi**, нажмите «Сканировать сети», выберите нужную сеть, введите пароль (или включите WPA2-Enterprise, если сеть корпоративная) и сохраните — устройство перезагрузится и подключится к ней.

## Веб-портал

| Страница | Назначение |
|---|---|
| `/` | статус: режим сети, тип безопасности, MAC-адрес, состояние сессии, статистика трафика, аптайм |
| `/settings` | пароль на сессию, White List, Static IP, скорость UART |
| `/wifi` | сканирование и выбор Wi-Fi сети, обычная сеть или WPA2-Enterprise, скорость UART, сброс настроек сети |

Основные действия доступны прямо с главной страницы: отключить зависшего клиента, перезагрузить устройство, сбросить Wi-Fi-настройки (уводит устройство обратно в режим точки доступа `192.168.4.1`). Язык интерфейса переключается кнопкой в правом верхнем углу.

## Подключение к консоли

```bash
telnet <ip-адрес-устройства> 8888
# или
nc <ip-адрес-устройства> 8888
```

Если включён пароль на сессию — устройство пришлёт `AUTH_REQUIRED`, в ответ нужно отправить пароль одной строкой.

Для PuTTY используйте тип соединения **Telnet**, а не Raw — так клиент правильно поймёт согласование эха от устройства и не будет дублировать набранные команды.

## WPA2-Enterprise

На странице `/wifi` включите переключатель «WPA2-Enterprise (802.1X)» и заполните:
- **Identity** — обычно совпадает с именем пользователя, формат зависит от организации (`user@domain` или `DOMAIN\user`);
- **Имя пользователя**;
- **Пароль**.

Поддерживается PEAP/MSCHAPv2 без проверки CA-сертификата — самый частый минимальный вариант корпоративных RADIUS-серверов. Если сеть требует EAP-TLS или строгую проверку сертификата сервера — эта схема не подойдёт без доработки.

## Компилируемые опции

В начале файла:

```cpp
// #define USE_BAN_LIST
```

Раскомментируйте, чтобы включить часовую блокировку IP-адреса после 3 неверных попыток пароля (работает только при включённом пароле на сессию).

## Безопасность — на что обратить внимание

- Пароль сессии и пароли Wi-Fi/Enterprise передаются/хранятся без шифрования — решение рассчитано на доверенную локальную сеть, не для доступа через интернет напрямую.
- Веб-портал не защищён паролем — ограничивайте доступ к нему на уровне сети (VLAN, ACL, White List).
- Сканирование Wi-Fi сетей на несколько секунд блокирует TCP-мост и веб-портал — учитывайте это при смене сети во время активной консольной сессии.

## Лицензия

Используйте, меняйте и распространяйте свободно — для личного проекта домашней/офисной инфраструктуры.
